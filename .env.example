DATABASE_URL="postgresql://quantblocks:quantblocks@localhost:5432/quantblocks?schema=public"
REDIS_URL="redis://localhost:6379"  # required: worker is the default paper mode + SSE push
NEXT_PUBLIC_PAPER_STREAM=true  # enable SSE push (requires paper-worker + Redis)
